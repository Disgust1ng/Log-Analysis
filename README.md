## Overview
This project is a simple Python script that analyses a log file to identify login activity.

## What it does
- Counts failed login attempts  
- Counts successful logins  
- Shows failed login attempts by user  

## How to run
1. Make sure Python is installed  
2. Place `logAnalysis.py` and `sample_log.txt` in the same folder  
3. Run:
